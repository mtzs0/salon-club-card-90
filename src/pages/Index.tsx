import { ClubCardForm } from "@/components/club-card/ClubCardForm";

const Index = () => {
  return (
    <div className="min-h-screen bg-muted flex items-center justify-center p-4">
      <ClubCardForm />
    </div>
  );
};

export default Index;
